# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# Instructions:
# 1. Copy this file to .env.local: cp .env.local.example .env.local
# 2. For local development: Use http://localhost:3001 (default, shown above)
# 3. For Docker development: Use http://backend:3001
# 4. Never commit .env.local to git (it's in .gitignore)
# 
# Note: The NEXT_PUBLIC_ prefix is required for Next.js to expose 
# the variable to the browser. Without it, the variable won't be available.
