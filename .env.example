# PostgreSQL Configuration
POSTGRES_USER=admin
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=shopdb

# Backend Configuration
DATABASE_HOST=postgres
DATABASE_PORT=5432
JWT_SECRET=GENERATE_WITH_generate-jwt-secret.js
JWT_EXPIRES_IN=1d
NODE_ENV=production

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Generate a secure JWT secret: node generate-jwt-secret.js
# 3. Replace GENERATE_WITH_generate-jwt-secret.js with the generated secret
# 4. Change POSTGRES_PASSWORD to a secure password
# 5. Never commit .env to git (it's in .gitignore)
